export const createScanSession = async (input: { placeName: string; description?: string }) => fetch('/api/scan/sessions', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ place_name: input.placeName, description: input.description })}).then(r=>r.json())
export const uploadScanFrame = async (input: { sessionId: string; imageB64: string; roomId?: string; guidanceHint?: string; ts?: number }) => fetch(`/api/scan/sessions/${input.sessionId}/frame`, {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ image_b64: input.imageB64, frame_id:'ios_camera', room_id: input.roomId, guidance_hint: input.guidanceHint, ts: input.ts ?? Date.now()/1000 })})
export const finishScanSession = async (sessionId: string) => fetch(`/api/scan/sessions/${sessionId}/finish`, {method:'POST'}).then(r=>r.json())
export const getScanSession = async (sessionId: string) => fetch(`/api/scan/sessions/${sessionId}`, {cache:'no-store'}).then(r=>r.json())
export const callDimosMcp = async (body: unknown) => fetch('/api/dimos/mcp', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(body)}).then(r=>r.json())
